@page

@model YAWYE.Pages.Products.ProductListModel
@{
}


<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="btn btn-default">
                <button class="btn">
                    <i class="btn btn-primary">Search</i>
                </button>
            </span>
        </div>
    </div>

</form>

    @foreach (var product in Model.Products)
    {
        <partial name="_Summary" model="product" />
        
    }
<a asp-page="./UpdateProduct" class="btn btn-primary">Edit</a>

<a asp-page="./AddProduct" class="btn btn-success">Add new</a>

